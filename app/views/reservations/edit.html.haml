%h1 Editing reservation

= error_messages_for :reservation

- form_for @reservation do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Update"

= link_to 'Show', object_path
|
= link_to 'Back', objects_path

%div[reservation]
  %p.gift_id
    %strong Gift
    = h reservation.gift
  %p.name
    %strong Name
    = h reservation.name
  %p.email
    %strong Email
    = h reservation.email

  = link_to 'Show', object_path(reservation)
  |
  = link_to 'Edit', edit_object_path(reservation)
  |
  = link_to 'Destroy', object_path(reservation), :confirm => 'Really destroy reservation?', :method => :delete
